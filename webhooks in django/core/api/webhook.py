import requests
import json

import requests
import json

# # define the URL of the webhook endpoint
# webhook_url = 'http://127.0.0.1:8005/webhook'

# # define the payload data as a dictionary
# payload = {'message': 'Hello, world!'}

# # encode the payload data as a JSON string
# payload_json = json.dumps(payload)

# # send an HTTP POST request to the webhook endpoint
# response = requests.post(webhook_url, data=payload_json)

# # print the response from the server
# print(response.text)


# # define the URL of the webhook endpoint
# webhook_url = 'http://127.0.0.1:8000/webhook'

# # define the payload data as a dictionary
# payload = {'message': 'Hello, world!'}

# # encode the payload data as a JSON string
# payload_json = json.dumps(payload)

# # send an HTTP POST request to the webhook endpoint
# response = requests.post(webhook_url, data=payload_json)

# # print the response from the server
# print(response.text)


