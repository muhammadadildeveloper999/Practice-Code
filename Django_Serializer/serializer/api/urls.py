from django.urls import path,include
from api.views import *


urlpatterns = [
    # path('validations', validations.as_view()),
    # path('verification', verification.as_view()),
    # path('login', login.as_view()),
    # path('ExampleView', ExampleView.as_view())
    
    
]